apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valheim-saves-pv-claim
  namespace: valheim
smepec:
  storageClassName: "valheim"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
